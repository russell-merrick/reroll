# Reroll — main app (Python 3.11+ / 3.14 OK)
# V0: no database. Scan → classify → generate → preview → export.

# Web API + static UI host
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Audio metadata (duration, sample rate, channels)
mutagen>=1.47.0

# MIDI write (V0.4+)
mido>=1.3.2

# Offline sample-loop stems for Ableton export
numpy>=1.26.0

# Optional later:
# soundfile>=0.12.1
# scipy>=1.14.0

# --- Serum VST host sidecar (separate env — Python 3.12 required) ---
# py -3.12 -m pip install dawdreamer numpy scipy serum2-preset-loader
# Serum 1: .fxp + Serum_x64.dll
# Serum 2: .SerumPreset + Serum2.vst3 via serum2-preset-loader
# See spikes/README.md
